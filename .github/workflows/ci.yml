name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: project
    steps:
      - uses: actions/checkout@v7
        with:
          path: project
      - name: Check out repository standards
        uses: actions/checkout@v7
        with:
          repository: lailai0916/lailai-template
          ref: aab624269fb9cdf18b9da5d11605eb9b0fc79154
          path: repository-standards
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'

      - name: Validate shared repository standards
        run: python3 ../repository-standards/scripts/check_repository.py --root . --display-name laiism.skill
      - name: Validate positions and project identity
        run: python3 scripts/check_repository.py
      - name: Test validation gates
        run: python3 -m unittest discover -s tests -v
      - name: Install Python development dependencies
        run: python3 -m pip install -r requirements-dev.txt
      - name: Check Python
        run: |
          python3 -m ruff check .
          python3 -m ruff format --check .
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: project/package-lock.json
      - run: npm ci --ignore-scripts
      - run: npm run format:check
